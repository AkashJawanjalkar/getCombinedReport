_schema-version: 3.3.0
ID: viatris_project
description: A simple CAP project.
version: 1.0.0
modules:
  - name: viatris_project-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: viatris_project-db
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: viatris_project-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: viatris_project-db
    parameters:
      buildpack: nodejs_buildpack
  - name: viatris_project-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: viatris_project-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - project1.zip
          name: project1
          target-path: resources/
        - artifacts:
            - project2.zip
          name: project2
          target-path: resources/
        - artifacts:
            - project3.zip
          name: project3
          target-path: resources/
        - artifacts:
            - project4.zip
          name: project4
          target-path: resources/
  - name: project1
    type: html5
    path: app/project1
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
  - name: project2
    type: html5
    path: app/project2
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
  - name: project3
    type: html5
    path: app/project3
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
  - name: project4
    type: html5
    path: app/project4
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: viatris_project-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: viatris_project-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: viatris_project-html5-service
      service-plan: app-host
  - name: viatris_project-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: viatris_project-destination-service
      service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
